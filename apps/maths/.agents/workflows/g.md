---
description: rules — પ્રોજેક્ટના master rules, દરેક task પહેલા auto-load
---

# 📜 /g — Master Rules

// turbo-all

## 1. Context (Auto-Load)
Read these skills before any work:
- `project-memory` → tech stack, design
- `file-index` → file paths
- `fast-worker` → speed rules
- `chapter-management` → chapter data formats (when chapter work)

## 2. Language & Format
- **ગુજરાતી** માં જવાબ — always
- Short, point format — no long paragraphs

## 3. Execution Mode

- Parallel tool calls → `fast-worker` skill follow
- Verify after changes (build / error check)

## 4. Coding Standards
- Theme:  Glassmorphism (see `project-memory`)
- CSS: `globals.css` only
- Logo: `/public/logo.png`
- Responsive: Mobile + Desktop
- Math content: **Unicode only, LaTeX BANNED** (see `chapter-management`)

## 5. File Discipline
- Paths → `file-index` skill use, NEVER search
- New/deleted file → `file-index` update
- Absolute paths always